# Sub-Agents Index

<!-- Sub-agents are atomic workers spawned BY agents — never by users directly.
     Add sub-agents only when a parent agent needs one for a specific atomic task.
     No ceiling on sub-agents.
-->

| Sub-agent | Spawned by | Job | AGENT.md |
|-----------|-----------|-----|----------|

_No sub-agents yet. Added as parent agents need them — not speculatively._
